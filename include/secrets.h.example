// secrets.h — copy to secrets.h and fill in your values for compile-time defaults.
// This file is git-ignored. Never commit secrets.h directly.
//
// NOTE: All of these can also be set at runtime via the captive portal (192.168.4.1)
// without reflashing. This file is only needed if you want compile-time defaults.

#pragma once

#define DEFAULT_WIFI_SSID     "your_wifi_ssid"
#define DEFAULT_WIFI_PASSWORD "your_wifi_password"
#define DEFAULT_SERVER_URL    "https://your-calendar-server/jsonCalendar?timeframe=1d"
#define DEFAULT_READ_TOKEN    ""   // leave empty if your server has no auth
#define DEFAULT_REFRESH_SECS  300
